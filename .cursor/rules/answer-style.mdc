---
description: 技术问题的默认回答结构
alwaysApply: true
---

# 回答风格（后续所有对话默认遵守）

用户已确认：按「先结论 → 逻辑主线 → 分段解释」写，不要跳跃堆砌。

## 结构（必须按这个顺序）

1. **先抛结论**  
   开头用一两句把答案说死，关键判断加粗。不要先铺背景、不要先甩名词表。

2. **给出逻辑主线**  
   用编号列出后面要讲的几步（为什么 → 是什么 → 按时间怎么走 → 边界）。主线是目录，不是正文。

3. **再分段解释**  
   严格按主线的编号展开。一节只讲一件事，讲完再进下一节。节与节之间用二级/三级标题分开。

## 写法

- 该分段就分段：一段只推进一个意思；定义、对比、时间顺序不要挤在同一段。
- 该加粗就加粗：结论里的判断、第一次出现的关键术语、禁止/必须做的约束。
- 该加大就加大：用 Markdown 标题做层次（`#` 主线，`##` 各段），不要用一长串短句+表格代替叙述。
- 先用完整句子把事讲清楚，表格只在对比已经讲过的对象时用。
- 术语第一次出现时用一句话解释，不要默认对方还记得上几轮的缩写。
- 和上一问有关系时，放在最后单独一节，不要在中间横跳。
- 公式、图表继续用 Markdown，不用 LaTeX。

## 不要

- 不要一上来并列很多卡片式短句、很多互不衔接的表格。
- 不要在正文中间穿插「见第 N 节 / PPT 第 N 页」代替解释。
- 不要用「一句话口径」收尾代替分段把道理讲完。
